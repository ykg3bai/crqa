#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main() {
    char name[8];
    int ready = 0;
    if(ready = 1);
    strcpy(name, "too long for the fixed buffer");
    malloc(32);
    printf("%s\n", name);
}
