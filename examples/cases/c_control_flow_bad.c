#include <stdio.h>
#include <math.h>

void main() {
    int unused = 0;
    int value = 1;

    if(value = 2);
    value == 3;

    for (int i = 0; i < 3; i++) { value += i; }

    if (value) {
        while (value) {
            if (value) {
                if (value) {
                    if (value) {
                        printf("%d\n", value);
                    }
                }
            }
        }
    }
}
